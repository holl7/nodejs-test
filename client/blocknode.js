'user strict'

const BaseDB = require("../db/basedb");
const EventEmitter=require("event");

/**
 * 这里会同步节点和节点数据
 */
class BlockNode extends EventEmitter{

    constructor(params){
        super();
        //if(params.udp)
    }




}